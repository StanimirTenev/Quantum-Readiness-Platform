{
  "name": "qrp-web-ui",
  "private": true,
  "version": "0.1.0",
  "description": "Buildless static frontend for the QRP API Gateway.",
  "scripts": {
    "dev": "python -m http.server 5173 --directory public",
    "build": "node scripts/build.js"
  }
}
